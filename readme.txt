the files in the 'test_files' folder can be used to see how the sorter works (obviously)
if there's no 'sorted' and 'unsorted' folders, just run the main.py (or main.exe) file once and it will generate them

- kurlwin
